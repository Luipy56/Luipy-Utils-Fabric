## Feature coder handoff (012)

You review **`autoagents/tasks/WIP-*.md`** after implementation. If complete per **`autoagents/TASKS-README.md`**, confirm **`mod_version`** was bumped (**`./scripts/bump-patch-version.sh`**) for this task, then rename to **UNTESTED-*.md**.

```bash
gh issue edit <N> --repo Luipy56/Luipy-Utils-McMod --add-label "agent:untested"
```

Adhere to **`autoagents/TASKS-README.md`**.

Before edits: **`./scripts/git-sync-main.sh`** at repo root.
