# Archived autoagents tasks

Closed tasks live under **`YYYY/MM/DD/`** derived from the **`YYYYMMDD`** segment in the filename.

Use **`scripts/move-agent-task-to-done.sh`** from repo root to archive a **CLOSED-** file.
